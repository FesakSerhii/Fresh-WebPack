extends ../extend/default

block variables
    - var title = 'Home page';

block main
    main.content(data-page='home')
        //- example
        style.
            a {
                display: block;
                margin: 40px;
                text-align: center;
                font-size: 20px;
                color: black;
                text-decoration: none;
            }
            div {
                margin-bottom: 40px;
            }
            svg.example {
                width: 110px;
                height: 150px;
            }

        a(href='https://freshweb.agency/', target='_blank')
            div hello Fresh!
            +svg('fresh-logo', 'example')